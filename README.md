# xinput devices locker

```
Usage:
     devlock.pl "DEVICE_ID_STRING" [position|last|first]
 or
     devlock.pl DEVICE_ID
 Default position of DEVICE_ID_STRING is "last"  
 Run `xinput --list` to get DEVICE_ID or DEVICE_ID_STRING
```